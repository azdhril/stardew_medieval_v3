---
phase: 05-dungeon
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - assets/Maps/dungeon_r1.tmx
  - assets/Maps/dungeon_r2.tmx
  - assets/Maps/dungeon_r3.tmx
  - assets/Maps/dungeon_r3a.tmx
  - assets/Maps/dungeon_r4.tmx
  - assets/Maps/dungeon_r4a.tmx
  - assets/Maps/dungeon_tileset.tsx
  - assets/Maps/village.tmx
  - src/World/DungeonDoor.cs
  - src/World/DungeonChestSeeder.cs
  - src/Scenes/DungeonScene.cs
  - src/Scenes/VillageScene.cs
  - src/World/ChestManager.cs
  - tests/Dungeon/DungeonRegistryTests.cs
  - tests/Dungeon/LootRollTests.cs
autonomous: true
requirements: [DNG-01, DNG-02, DNG-03]
tags: [dungeon, tmx, door, chest, trigger]
must_haves:
  truths:
    - "Player walks to dungeon entrance in village.tmx and transitions into dungeon room 1"
    - "Player navigates all 6 non-boss rooms via triggers (r1→r2→r3→r4, r3↔r3a, r4↔r4a)"
    - "Closed doors block player collision; opened doors allow passage and enable exit trigger"
    - "Clearing a main room opens its exit door (visible sprite swap)"
    - "Optional rooms contain chests; chests seed contents from DungeonState.ChestContents on dungeon entry"
    - "Chest Take-All does not re-roll on re-entry"
  artifacts:
    - path: "assets/Maps/dungeon_r1.tmx"
      provides: "Entrance room TMX with Triggers object group + EnemySpawns + Door tile"
    - path: "src/World/DungeonDoor.cs"
      provides: "Entity with IsOpen, sprite swap, CollisionBox toggle"
      contains: "DungeonDoor"
    - path: "src/Scenes/VillageScene.cs"
      provides: "enter_dungeon trigger routes to DungeonScene r1"
    - path: "src/Scenes/DungeonScene.cs"
      provides: "Full room lifecycle: spawn enemies, manage doors, seed chests, handle exits"
  key_links:
    - from: "assets/Maps/village.tmx"
      to: "src/Scenes/DungeonScene.cs"
      via: "TriggerZone 'enter_dungeon' → BeginDungeonRun + TransitionTo DungeonScene(r1)"
    - from: "src/Scenes/DungeonScene.cs"
      to: "src/World/DungeonDoor.cs"
      via: "DungeonScene.GetSolids() includes closed doors"
    - from: "src/Scenes/DungeonScene.cs"
      to: "src/World/DungeonState.cs"
      via: "ChestContents seeded once per run, re-read on room entry"
---

<objective>
Author the 6 non-boss room TMX files, the `DungeonDoor` entity with collision-gated sprite swap, chest seeding on dungeon entry (idempotent across room re-entries), and the village → dungeon entry trigger. After this plan, a player can enter the dungeon from the village, fight through main rooms (doors gated on clear), detour into optional rooms to open chests, and return to village if they leave via r1's exit — everything except the boss room.

Purpose: deliver the explorable content layer on top of the infrastructure from Plan 01. Zero new systems — only composition of TMX + `DungeonDoor` + chest wiring.

Output: 6 playable dungeon rooms with working gates, chests, and round-trip to village.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dungeon/05-CONTEXT.md
@.planning/phases/05-dungeon/05-RESEARCH.md
@.planning/phases/05-dungeon/05-01-SUMMARY.md

@assets/Maps/village.tmx
@assets/Maps/castle.tmx
@src/Scenes/VillageScene.cs
@src/Scenes/DungeonScene.cs
@src/Scenes/ChestScene.cs
@src/World/ChestInstance.cs
@src/World/ChestManager.cs
@src/World/TriggerZone.cs
@src/Data/DungeonRegistry.cs
@src/World/DungeonState.cs

<interfaces>
From src/World/ChestInstance.cs:
- `ChestInstance(string id, Point tile, string spriteId)`, `bool IsOpen`, `void Open()`, `List<ItemStack> Inventory`.

From src/World/ChestManager.cs:
- `ChestInstance? GetChestAtFacingTile(PlayerEntity player)`, `List<ChestInstance> Chests`, `Draw(SpriteBatch, Texture2D)`.

From src/Scenes/VillageScene.cs.HandleTrigger:
- Existing pattern: `case "castle_door": TransitionTo(new CastleScene(Services, "village")); return true;`.

From assets/Maps/village.tmx:
- Has an object group named `Triggers` with Rectangle objects carrying `name` property.
- Tile layers use `farm_tileset.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DungeonDoor entity + dungeon tileset + village entry trigger</name>
  <files>src/World/DungeonDoor.cs, src/Scenes/VillageScene.cs, src/Scenes/DungeonScene.cs, assets/Maps/dungeon_tileset.tsx, assets/Maps/village.tmx</files>
  <action>
1. **Create `src/World/DungeonDoor.cs`**:
   - `public class DungeonDoor : Entity`
   - Ctor: `DungeonDoor(string doorId, string targetRoomId, Rectangle closedBounds, Texture2D? sprite)`.
   - Fields: `_closedBounds`, `_sprite`, `IsOpen` (public get, private set).
   - `public override Rectangle CollisionBox => IsOpen ? Rectangle.Empty : _closedBounds;`
   - `public void Open()` — sets `IsOpen=true`, logs `[DungeonDoor] {doorId} opened (→ {targetRoomId})`.
   - `public void Close()` — mirror for future use (post-death reset).
   - `Draw(SpriteBatch sb)` — if sprite provided, swap source rect between open/closed frames (columns 0/1 of a 2-frame sheet). If sprite null, draw colored rectangle per fallback in RESEARCH §Environment (closed = red tint, open = green tint).
   - Per CLAUDE.md: `/// <summary>` docstring, `_underscore` fields, PascalCase.

2. **Create `assets/Maps/dungeon_tileset.tsx`** as a minimal Tiled tileset referencing existing `Buildings/3_Props_and_Buildings_16x16.png` + a flat grey floor tile (8×8 tiles minimum). Use the existing `farm_tileset.tsx` as the format template.

3. **Update `assets/Maps/village.tmx`**: add a new Trigger object named `enter_dungeon` (Rectangle AABB) at an unoccupied visual spot (e.g., near map edge opposite shop). Add a small "cave entrance" decoration tile at that location. Use Tiled-compatible XML; manual edit acceptable per asset README.

4. **Update `src/Scenes/VillageScene.HandleTrigger`**: add case:
```csharp
case "enter_dungeon":
    BeginDungeonRun(Services);   // new method defined below
    Services.SceneManager!.TransitionTo(new DungeonScene(Services, "r1", "village"));
    return true;
```
Add `private static void BeginDungeonRun(ServiceContainer svc)` helper that:
- Ensures `svc.Dungeon != null`; if null, constructs one.
- Calls `svc.Dungeon!.BeginRun()`.
- Seeds chest contents (loot-roll logic extracted in Task 2; for now stub-call into a new static `DungeonChestSeeder.Seed(svc)` method).
- Logs `[VillageScene] Entering dungeon run {seed}`.

5. **Update `src/Scenes/DungeonScene`** to:
   - Maintain `_doors` list populated in `OnLoad` from TMX object group `Doors` (see Task 2).
   - Include closed doors in `GetSolids()` via `_doors.Where(d => !d.IsOpen).Select(d => d.CollisionBox)` so `PlayerEntity.Update` collision check consults them (Pitfall 7).
   - In room-cleared block (already wired in Plan 01), iterate `_doors` and call `door.Open()` for doors whose `TargetRoomId` matches the next-room exit for this room.
  </action>
  <verify>
    <automated>dotnet build stardew_medieval_v3.csproj --nologo</automated>
  </verify>
  <done>`DungeonDoor` compiles, collision-box toggles with `IsOpen`. `village.tmx` contains `enter_dungeon` trigger. `VillageScene.HandleTrigger` routes to DungeonScene r1 and calls BeginDungeonRun. Game boots, player can walk to trigger (no dungeon maps yet → loading r1 will fail — acceptable, Task 2 adds TMXs).</done>
</task>

<task type="auto">
  <name>Task 2: Author 6 room TMXs + chest seeding + ChestManager per-scene scoping</name>
  <files>assets/Maps/dungeon_r1.tmx, assets/Maps/dungeon_r2.tmx, assets/Maps/dungeon_r3.tmx, assets/Maps/dungeon_r3a.tmx, assets/Maps/dungeon_r4.tmx, assets/Maps/dungeon_r4a.tmx, src/Scenes/DungeonScene.cs, src/World/ChestManager.cs, src/Data/DungeonRegistry.cs, tests/Dungeon/LootRollTests.cs, tests/Dungeon/DungeonRegistryTests.cs</files>
  <action>
1. **Create 6 TMX files** (hand-authored per D-03 using `castle.tmx` as reference for structure):
   - Each TMX: 20×15 tiles (matching 960×540 @ 32px = half-scale — actually current project uses 16px tiles, use 30×17 to match ~480×270 map that scrolls; match format of `test_farm.tmx`). Inspect `test_farm.tmx` for exact conventions.
   - Each TMX has these object groups:
     * `Triggers` — Rectangle objects with `name` matching `DungeonRegistry.Rooms[id].Exits.Keys` (e.g., `exit_r1_to_r2`, `exit_r1_to_village`).
     * `EnemySpawns` — Point objects with property `enemyId` (string). DungeonScene reads these in `OnLoad` to build the spawn list — falls back to `DungeonRegistry.Rooms[id].Spawns` if empty, so either authoring path works.
     * `Doors` — Rectangle objects with properties `doorId` (string) and `targetRoomId` (string). Used to instantiate `DungeonDoor` entities. Position is closed-collision AABB.
     * `ChestSpawns` — Point objects with property `chestId` (string) — optional rooms only.
     * `Spawn` — single Point object `from_<prev>` giving the player spawn when entering from that direction (mirrors VillageScene's pattern).
   - Rooms:
     * `dungeon_r1.tmx`: simple rectangular room with a door on the north wall (exit to r2) and south wall trigger (exit to village).
     * `dungeon_r2.tmx`: north door to r3.
     * `dungeon_r3.tmx`: north door to r4 (gated), east trigger/door to r3a (ungated).
     * `dungeon_r3a.tmx`: small side room, chest, west exit back to r3.
     * `dungeon_r4.tmx`: north door to boss (gated, checks ALL main rooms cleared — see Task-level logic below), east trigger to r4a (ungated).
     * `dungeon_r4a.tmx`: chest + 2 Skeleton spawns, west exit back to r4.

   Use placeholder visuals: grey floor tile + wall tile from `Buildings/3_Props_and_Buildings_16x16.png`. This is acceptable per RESEARCH §Open-Questions Q3.

2. **Extend `DungeonScene.OnLoad`** to:
   - Read TMX object groups via `TileMap.ObjectGroups` (use existing API — check `TileMap.cs` for exact method name when implementing).
   - Build enemy spawn list from `EnemySpawns` object group; fallback to `_room.Spawns` if group absent.
   - Build doors from `Doors` group → instantiate `DungeonDoor` each, add to `_doors`.
   - Build chests from `ChestSpawns` group → for each, read `chestId`, look up content from `Services.Dungeon!.ChestContents[chestId]` (empty list if missing — Pitfall 5 idempotency), construct `ChestInstance`, add to the per-scene `ChestManager`. If chest is already in `Services.Dungeon.OpenedChestIds`, pre-mark it open.
   - Read player spawn from `Spawn` object group matching the `_fromScene` argument (e.g., `from_r2` when entering r3 from r2). Fallback: top-left walkable tile.

3. **Create `src/World/DungeonChestSeeder.cs`** (new file, extract from Plan 01's Task 2 stub):
```csharp
public static class DungeonChestSeeder
{
    public static void Seed(ServiceContainer svc)
    {
        var dungeon = svc.Dungeon!;
        var rng = new Random(dungeon.RunSeed);
        var table = new LootTable(new List<LootDrop>
        {
            new("Health_Potion", 1.0f),
            new("Iron_Sword",    0.25f),
            new("Mana_Crystal",  0.4f),
            new("Gold_Coin",     0.9f),
        });
        foreach (var room in DungeonRegistry.GetAll())
        foreach (var (chestId, _, _) in room.Chests)
            dungeon.ChestContents[chestId] = table.Roll(rng)
                .Select(d => d.ItemId).ToList();
        Console.WriteLine($"[DungeonChestSeeder] Seeded {dungeon.ChestContents.Count} chest(s) with seed {dungeon.RunSeed}");
    }
}
```
Call it from `VillageScene.BeginDungeonRun` (replacing the Task 1 stub call).

4. **Update `ChestManager`** if needed so `Services.ChestManager` can be swapped per scene without breaking FarmScene. Verify the existing ChestManager is per-instance (already is per Plan 01 analysis). No code change expected — just wire `Services.ChestManager = _chestManager` in `DungeonScene.OnLoad` and restore to FarmScene's on exit.

5. **DungeonRegistry update** — encode boss prerequisite in `r4.Exits["exit_r4_to_boss"]`: `RequiresCleared = true` AND a special flag `RequiresAllMainRoomsCleared = true`. Extend `ExitData` record with this flag. `DungeonScene.HandleTrigger` checks it against `Services.Dungeon!.ClearedRooms` containing `r1, r2, r3, r4`.

6. **Fill `LootRollTests`**: call `DungeonChestSeeder.Seed` with a fixed seed twice, assert `ChestContents` is equal (determinism). `DungeonRegistryTests`: assert every `Exits` key in registry matches a string found in its target TMX `Triggers` object group (parse TMX XML in test with `System.Xml.Linq`).
  </action>
  <verify>
    <automated>dotnet build stardew_medieval_v3.csproj --nologo && dotnet test tests/stardew_medieval_v3.Tests/stardew_medieval_v3.Tests.csproj --filter "FullyQualifiedName~Dungeon" --nologo</automated>
  </verify>
  <done>All 6 TMX files load without TiledCS exception. Game boots, player walks from village to dungeon entrance, enters r1, fights enemies, room clears, door opens, advances to r2. Detours into r3a/r4a open chests (ChestScene overlay shows loot from seeded contents). LootRollTests proves seed determinism. DungeonRegistryTests proves no orphan trigger names across all 6 TMXs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → `TileMap.Load` | Dungeon TMX files treated as trusted (shipped), but parse-time exceptions must be caught. |
| Disk → loot seed JSON | `ChestContents` deserialized from save; a hand-edited save could inject arbitrary item ids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | TMX parser → DungeonScene.OnLoad | accept | Assets shipped with game; parse failure throws loudly at startup. No remote TMX loading. |
| T-05-06 | Elevation of Privilege | Hand-edited save injects item ids into ChestContents | mitigate | `ChestInstance.Open()` resolves item ids via `ItemRegistry.Get` — unknown ids log warning and are skipped. No arbitrary code path. |
| T-05-07 | Denial of Service | Missing `EnemySpawns` / `Doors` object group in TMX | mitigate | DungeonScene falls back to `DungeonRegistry.Rooms[id].Spawns` and empty door list; logs `[DungeonScene] Warning: group missing`. Room remains playable. |
</threat_model>

<verification>
- All 6 dungeon TMXs parse via TiledCS.
- Manual smoke: village → dungeon r1 → kill → door opens → r2 → r3 → r3a detour → open chest → back → r3 → r4 → r4a → r4 → attempt to reach boss door blocked until r4 cleared.
- Chest contents deterministic from RunSeed; do not re-roll on room re-entry.
- `dotnet test` green on Dungeon + Loot + Registry filters.
</verification>

<success_criteria>
1. Player can enter dungeon from village via `enter_dungeon` trigger.
2. 6 rooms connect via triggers matching `DungeonRegistry.Rooms[i].Exits`.
3. Doors block passage when closed, open on room clear, sprite swaps visibly.
4. Optional rooms have chests that use `ChestScene` drag-and-drop (existing overlay, zero new UI).
5. Chest contents generated once per run; re-entering room preserves state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dungeon/05-02-SUMMARY.md`: rooms authored, doors working, chest seeding idempotent, TMX object-group contract locked, interfaces available for Plan 03 (boss).
</output>
