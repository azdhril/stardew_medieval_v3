---
phase: 05-dungeon
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - assets/Maps/dungeon_boss.tmx
  - src/Scenes/DungeonScene.cs
  - src/Combat/BossEntity.cs
  - src/Combat/EnemySpawner.cs
  - tests/Dungeon/DungeonStateTests.cs
  - tests/Dungeon/BossVictoryTests.cs
autonomous: true
requirements: [DNG-04, DNG-01]
tags: [dungeon, boss, quest, victory]
must_haves:
  truths:
    - "Player entering boss room (r4 → boss gate cleared) spawns the BossEntity"
    - "Defeating the boss drops guaranteed loot as ItemDropEntity and sets DungeonState.BossDefeated=true"
    - "Boss defeat fires Services.Quest.Complete() flipping MainQuest to Complete"
    - "After boss death the exit to village activates; player returns to village near castle_door"
    - "King NPC dialogue reflects Complete state (exercises Phase 4 NPC-04 branch)"
    - "Re-entering boss room after defeat does not respawn the boss"
  artifacts:
    - path: "assets/Maps/dungeon_boss.tmx"
      provides: "Boss room map with BossSpawn + exit_boss_to_village trigger"
    - path: "src/Scenes/DungeonScene.cs"
      provides: "Boss spawn guard + victory handler"
    - path: "tests/Dungeon/BossVictoryTests.cs"
      provides: "Unit coverage for quest complete + save roundtrip"
  key_links:
    - from: "src/Scenes/DungeonScene.cs (boss room)"
      to: "src/Quest/MainQuest.cs"
      via: "Services.Quest.Complete() on boss death"
    - from: "src/Scenes/DungeonScene.cs"
      to: "src/Scenes/VillageScene.cs"
      via: "TransitionTo VillageScene with spawn 'castle_door'"
---

<objective>
Add the boss room — the climax that flips MainQuest to Complete and returns the player to the village for quest-complete dialogue. Boss reuses existing `BossEntity` (no generalization), spawn is guarded by `DungeonState.BossDefeated`, victory spawns guaranteed loot via `ItemDropEntity` (not a chest), and the exit trigger returns the player to village at the castle door (per D-14 decision locked in Plan 01).

Purpose: close the dungeon loop end-to-end. After this plan, all four success criteria for Phase 5 are met (5-8 rooms ✓, gated progression ✓, treasure chests ✓, boss defeat completes objective ✓).

Output: a fully playable dungeon from entrance to victory, quest completion, and return-to-village.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dungeon/05-CONTEXT.md
@.planning/phases/05-dungeon/05-RESEARCH.md
@.planning/phases/05-dungeon/05-01-SUMMARY.md
@.planning/phases/05-dungeon/05-02-SUMMARY.md

@src/Combat/BossEntity.cs
@src/Combat/EnemySpawner.cs
@src/Scenes/DungeonScene.cs
@src/Quest/MainQuest.cs
@src/Scenes/VillageScene.cs
@src/Scenes/CastleScene.cs

<interfaces>
From src/Combat/BossEntity.cs:
- `BossEntity(Vector2 position, ContentManager content)` — ctor verified.
- `bool IsAlive`, `int HP`, `Update(gameTime, playerPos, enemies)`, `List<(string itemId, int qty)> GetBossLoot(bool priorKill)`.

From src/Combat/EnemySpawner.cs (post-Plan 01 refactor):
- `public BossEntity SpawnBoss(Vector2 position)`.

From src/Quest/MainQuest.cs:
- `void Complete()` — flips state, raises `OnStateChanged`.

From src/Data/DungeonRegistry.cs (post-Plan 02):
- `Rooms["boss"].IsBossRoom == true`, `Exits["exit_boss_to_village"] = new(LeaveDungeon: true, TargetScene: "village", TargetTrigger: "castle_door")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author dungeon_boss.tmx + boss spawn guard + victory handler</name>
  <files>assets/Maps/dungeon_boss.tmx, src/Scenes/DungeonScene.cs, src/Scenes/VillageScene.cs</files>
  <action>
1. **Create `assets/Maps/dungeon_boss.tmx`** — larger room (~30×20 tiles for boss space). Object groups:
   - `Triggers`: one Rectangle named `exit_boss_to_village` placed near south wall, INITIALLY inactive/behind a closed `DungeonDoor`.
   - `Doors`: one Rectangle for the exit door, property `doorId=boss_exit`, `targetRoomId=village`.
   - `BossSpawn`: single Point object at room center with property `bossId=skeleton_king`.
   - `Spawn` object `from_r4` at north entrance.
   - No `EnemySpawns` (boss handles its own minions via summon phases).
   - Use distinctive placeholder visuals — darker floor tile to signal boss arena.

2. **Extend `DungeonScene`** with boss-room branch in `OnLoad`:
```csharp
if (_room.IsBossRoom)
{
    if (!Services.Dungeon!.BossDefeated)
    {
        var bossTile = ReadBossSpawn(tileMap); // read BossSpawn Point group
        _boss = _spawner.SpawnBoss(bossTile);
        Console.WriteLine($"[DungeonScene:boss] Boss spawned at {bossTile}");
    }
    else
    {
        // Re-entry after victory — open exit door immediately, no boss
        foreach (var d in _doors) d.Open();
        Console.WriteLine("[DungeonScene:boss] Re-entry — boss already defeated");
    }
}
```

3. **Extend `DungeonScene.OnPreUpdate`** with victory handler AFTER `CombatLoop.Update`:
```csharp
if (_room.IsBossRoom && _boss != null && !_boss.IsAlive)
{
    Console.WriteLine("[DungeonScene:boss] Boss defeated!");
    var loot = _boss.GetBossLoot(priorKill: false);
    foreach (var (id, qty) in loot)
        SpawnItemDrop(id, qty, _boss.Position); // uses existing FarmScene helper pattern

    Services.Dungeon!.BossDefeated = true;
    Services.Quest?.Complete();

    foreach (var d in _doors) d.Open(); // unlock exit

    GameStateSnapshot.SaveNow(Services); // persist victory
    _boss = null;
}
```

4. **Gate boss room entry**: in `DungeonScene.HandleTrigger`, when the current room is `r4` and `target.RequiresAllMainRoomsCleared`, verify `Services.Dungeon!.ClearedRooms.IsSupersetOf(new[]{"r1","r2","r3","r4"})`. If not, return false with log `[DungeonScene] Boss door locked — clear all main rooms first`.

5. **Extend `VillageScene`** to honor `TargetTrigger`-based spawn on entry: when transitioning from DungeonScene with `TargetTrigger=="castle_door"`, set player position to the `Spawn` object matching `from_castle_door` (or fallback: in front of castle door). VillageScene already handles this pattern for castle ↔ shop entry; extend it to recognize the new `from_dungeon` spawn. Add a `Spawn` object named `from_dungeon` in `village.tmx` near the dungeon entrance so returning player stays coherent; note the D-14 decision routes them near castle_door specifically — use `from_castle_door` spawn (already exists from Phase 4) or add a new `from_dungeon` Point near the castle door.

6. End-run bookkeeping: when player leaves boss room via `exit_boss_to_village`, call `Services.Dungeon!.EndRun()` before the scene transition so the dungeon is NOT still "active" in the save. `BossDefeated=true` remains (it's a persistent milestone), but `IsRunActive=false` and `ClearedRooms` can stay (they're harmless post-victory since re-entering the dungeon will re-call `BeginRun()`).
  </action>
  <verify>
    <automated>dotnet build stardew_medieval_v3.csproj --nologo</automated>
  </verify>
  <done>`dungeon_boss.tmx` parses. Launching the game, clearing all 4 main rooms, and entering the boss room spawns `BossEntity` in the center. Defeating the boss drops items, fires `Quest.Complete()`, opens the exit door, and `SaveNow` persists the `BossDefeated=true` flag. Leaving returns to village at castle door.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BossVictoryTests + DungeonStateTests expansion + end-to-end unit assertions</name>
  <files>tests/Dungeon/BossVictoryTests.cs, tests/Dungeon/DungeonStateTests.cs</files>
  <behavior>
    - `BossVictoryTests.OnBossDeath_QuestCompleted`: simulate the boss-defeat code path; assert `MainQuest.State == Complete`, `DungeonState.BossDefeated == true`.
    - `BossVictoryTests.BossVictory_Persists_AcrossSaveRoundtrip`: save via `GameStateSnapshot.SaveNow`, reload via `SaveManager.Load`, assert `BossDefeated` survives.
    - `BossVictoryTests.ReEntry_AfterDefeat_DoesNotRespawnBoss`: construct a DungeonScene-esque fixture (or test a small `BossSpawnGate.ShouldSpawn(DungeonState)` helper extracted from the OnLoad branch) that returns `false` when `BossDefeated==true`.
    - `DungeonStateTests.DeathResetsRun_BeginRunCalled`: assert that after `BeginRun()`, `ClearedRooms`, `OpenedChestIds`, `ChestContents` are all empty AND `BossDefeated` does not reset (milestone stays once earned across save/load — confirm against CONTEXT D-13 which says "dungeon reseta completamente"; if D-13 requires BossDefeated to reset on death, flip the assertion and reset it in `BeginRun` too — re-read D-13 during implementation).
  </behavior>
  <action>
**Decision check on D-13 re-read:** D-13 says "dungeon reseta completamente (inimigos respawnam, baús refecham, portas refecham, loot re-rolla)". It does NOT mention boss. D-14 says "Derrotar o boss = dungeon completa" — i.e., boss-defeat is the terminal state, not something you redo. Therefore: **`BossDefeated` is a persistent milestone and should NOT reset inside `BeginRun()`**. Lock this in the test and in a comment on `DungeonState.BeginRun`.

1. Extract `public static bool BossSpawnGate.ShouldSpawn(DungeonState state) => !state.BossDefeated;` as a testable helper (small file `src/Combat/BossSpawnGate.cs` or inline static method on `DungeonState`). Use it from DungeonScene's OnLoad branch.

2. Write tests per behavior above. Use fakes for `ServiceContainer` where needed — or construct minimal real instances (ServiceContainer is a simple POCO per RESEARCH).

3. For the save-roundtrip test: write JSON to `Path.GetTempFileName()`, call SaveManager.Load on it, compare flags.

4. Add a test `DungeonRegistryTests.BossRoom_RequiresAllMainRoomsCleared`: asserts the Registry's boss entrance exit has `RequiresAllMainRoomsCleared=true` and all four main room ids (`r1..r4`) are present in `Rooms`.

5. Update test trait to `[Trait("Category", "quick")]` across all new tests to keep the quick filter comprehensive.
  </action>
  <verify>
    <automated>dotnet test tests/stardew_medieval_v3.Tests/stardew_medieval_v3.Tests.csproj --filter "FullyQualifiedName~Boss|FullyQualifiedName~Dungeon" --nologo</automated>
  </verify>
  <done>BossVictoryTests green. DungeonStateTests DeathResetsRun asserts the correct reset semantics (BossDefeated persists across `BeginRun`). All Dungeon-filter tests pass. Manual UAT playthrough (village → dungeon → 4 main rooms + 2 side rooms with chests → boss → village → King dialogue reflects Complete) documented in 05-03-SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → save | `BossDefeated=true` in save grants quest-complete state; a hand-edited save could skip the fight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Save-file edit to set BossDefeated=true | accept | Single-player local game; cheating the save is the user's own save. Not a security boundary. |
| T-05-09 | Repudiation | Quest.Complete fires but save fails | mitigate | `GameStateSnapshot.SaveNow` wraps write in try/catch (existing SaveManager behavior); log error to console. Next quit+save will re-persist. Acceptable for local single-player. |
| T-05-10 | Denial of Service | BossSpawn object group missing from dungeon_boss.tmx | mitigate | DungeonScene fallback: spawn boss at map center if BossSpawn group absent. Log `[DungeonScene:boss] Warning: BossSpawn missing, using map center`. |
</threat_model>

<verification>
- Full playthrough: Enter dungeon from village → clear r1 → r2 → r3 (detour r3a, open chest) → r4 (detour r4a) → enter boss → defeat → return to village → talk to King → quest-complete dialogue fires.
- Reloading save after boss defeat: `BossDefeated=true` persists.
- Re-entering dungeon after victory: room flags reset (can re-explore), but boss does not respawn.
- `dotnet test` full suite green.
</verification>

<success_criteria>
1. Boss room only enterable after all 4 main rooms cleared (trigger blocks otherwise).
2. Boss spawns on first entry per run; does not respawn after `BossDefeated=true`.
3. Boss defeat drops `GetBossLoot()` as `ItemDropEntity` (not chest).
4. Boss defeat calls `Services.Quest.Complete()`; King NPC dialogue reflects Complete state.
5. Exit door opens post-defeat; player returns to village at castle door position.
6. Save/load preserves `BossDefeated` milestone across sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dungeon/05-03-SUMMARY.md`: boss room authored, quest complete hook wired, victory persistence confirmed, full Phase 5 acceptance playthrough documented.
</output>
