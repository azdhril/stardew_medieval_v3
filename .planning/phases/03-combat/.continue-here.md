---
context: phase
phase: 03-combat
task: 3
total_tasks: 3
timestamp: 2026-04-12T02:30:00Z
---

# Resume Point: Item Registry Overhaul (mid-Phase 03 polish)

## Where we are

Phase 03 (combat) is technically done. User asked for a polish pass on the item system
on top of the phase. All changes compile cleanly; nothing has been committed yet.

## What landed this session (uncommitted)

1. **SpriteAtlas rewrite** — `Pickup_Items.png` grid is actually 14 cols with pairs
   (crop + crop_rotten). Previous packed mapping was wrong for everything past col 0.
   Sheet has 1-row vertical gaps between content bands: rows 0–2 crops, row 3 gap,
   row 4 tree fruits (skip), row 5 fish+boot+staff, row 6 gap, row 7 wood/stars.
2. **Food_Icons.png** loaded in `FarmScene` + new `RegisterFoodIcons()` method with
   13 seed registrations for existing crops.
3. **items.json** — 15 `*_Rotten` crops, new `Magic_Staff` weapon
   (`damage:5, spell:1, cooldown:1.0`), swords tagged `melee:1`, `Leather_Boots`
   repointed to `armor_leather_boots`.
4. **Wilted harvest drops rotten** — `ToolController.TryHarvest` spawns
   `{YieldItemName}_Rotten` if registered. Brown tint removed from
   `GridManager.DrawCrops`.
5. **Combat weapon switch** — `CombatManager.HandleInput` branches on active
   hotbar weapon: `spell:1` casts fireball (cooldown from Stats), else melee.
   RMB fireball removed. HUD F indicator only shows while cooling down.
6. **Per-seed planting** — `CropManager.TryPlant(tile, cropName?)` accepts
   explicit crop name. `ToolController.TryPlantActiveSeed` derives from
   `Cabbage_Seed` → `Cabbage`, consumes 1 seed via new
   `InventoryManager.TryConsume`, clears hotbar ref when stack empties.
7. **ItemDropEntity** stores `_spriteTexture` so rotten/seed/tool drops render from
   the correct backing sheet (not just primary).
8. **F2 debug kit** now includes `Magic_Staff`, `Leather_Boots`, `Tomato_Seed`.

## Why paused

Context window at 90%. User needs to playtest before commit. Two bugs already
fixed mid-session (all seeds planted same crop; F2 missed staff) but sprite
positions for boot/staff were wrong and corrected (row 4 → row 5). Whether
rows 0–2 are truly contiguous (no gaps) is unconfirmed — needs eyeballing.

## Next steps on resume

1. **User playtests**:
   - F2 → check sprites: cabbage/radish/carrot/etc and their rotten variants all
     look right in inventory. Boot and staff show correct icons.
   - Put 3 different seeds in hotbar slots 0/1/2. Till. Plant each. 3 distinct
     crops should grow. Seed stack decrements on use.
   - Equip Iron_Sword → LMB = melee. Equip Magic_Staff → LMB = fireball (1s cd).
     HUD F indicator appears only during the 1s cooldown.
   - Let a crop wilt (plant, don't water, advance days). Harvest with scythe or
     hands. A `{Crop}_Rotten` drop should spawn and be collectible.
2. **If sprites are still off** for crops: the gap theory may extend further
   (e.g. rows 1–2 may actually be at sheet rows 2, 4). Shift `crop_*` row
   indices in `SpriteAtlas.CreateDefault` accordingly.
3. **Commit** the batch (split or single) once playtest passes.
4. **Then** resume original Phase 03 backlog: full CMB-01..CMB-06 playtest →
   `/gsd-verify-phase 3`.

## Key files touched

- `src/Data/SpriteAtlas.cs`
- `src/Data/items.json`
- `src/Combat/CombatManager.cs`
- `src/Entities/ItemDropEntity.cs`
- `src/Farming/CropManager.cs`
- `src/Farming/GridManager.cs`
- `src/Farming/ToolController.cs`
- `src/Inventory/InventoryManager.cs`
- `src/Scenes/FarmScene.cs`
- `src/UI/HUD.cs`
