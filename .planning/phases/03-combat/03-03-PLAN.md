---
phase: 03-combat
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - Combat/BossEntity.cs
  - Combat/EnemySpawner.cs
  - Scenes/FarmScene.cs
  - Core/GameState.cs
  - Core/SaveManager.cs
autonomous: false
requirements:
  - CMB-06

must_haves:
  truths:
    - "A boss enemy (Skeleton King) spawns at a fixed position on the farm as a large red rectangle"
    - "Boss performs a wide slash attack with 1-second red flash wind-up before dealing 25 damage"
    - "Boss summons 2 skeleton minions when crossing 70% and 40% HP thresholds"
    - "Boss displays a large HP bar at the bottom of the screen with name 'Skeleton King'"
    - "Defeating the boss for the first time drops Flame_Blade (guaranteed) plus gold bonus"
    - "BossKilled flag persists across save/load"
  artifacts:
    - path: "Combat/BossEntity.cs"
      provides: "Boss with telegraphed attacks, summon phases, unique loot"
      contains: "class BossEntity"
    - path: "Core/GameState.cs"
      provides: "BossKilled flag for first-kill loot tracking"
      contains: "BossKilled"
  key_links:
    - from: "Combat/BossEntity.cs"
      to: "Combat/EnemyEntity.cs"
      via: "Extends EnemyEntity or Entity with boss-specific attack patterns"
      pattern: "class BossEntity"
    - from: "Scenes/FarmScene.cs"
      to: "UI/BossHealthBar.cs"
      via: "Draws boss HP bar when boss is alive"
      pattern: "BossHealthBar.Draw"
    - from: "Combat/BossEntity.cs"
      to: "Core/GameState.cs"
      via: "Checks/sets BossKilled flag for first-kill loot"
      pattern: "BossKilled"
---

<objective>
Create the Skeleton King boss fight with telegraphed attacks, minion summoning at HP thresholds, boss HP bar UI, unique loot drops, and save/load integration for first-kill tracking.

Purpose: The boss is the culmination of the combat phase -- a test of all combat mechanics working together. It validates that melee, projectiles, HP bars, knockback, and AI function in a challenging encounter.
Output: BossEntity class, boss integration into FarmScene, GameState expansion for BossKilled flag, visual checkpoint to verify the full combat experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-combat/03-CONTEXT.md
@.planning/phases/03-combat/03-RESEARCH.md
@.planning/phases/03-combat/03-01-SUMMARY.md
@.planning/phases/03-combat/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From Combat/EnemyEntity.cs (Plan 02):
```csharp
public class EnemyEntity : Entity
{
    public EnemyEntity(EnemyData data, Vector2 spawnPosition);
    public EnemyData Data { get; }
    public EnemyAI AI { get; }
    public bool IsMeleeAttackReady { get; }
    public void ConsumeMeleeAttack();
    public void Update(float deltaTime, Vector2 playerPos, ProjectileManager projectiles);
    public override void Draw(SpriteBatch spriteBatch);
}
```

From Combat/EnemyData.cs (Plan 02):
```csharp
public record EnemyData(string Id, string Name, Color PlaceholderColor, int Width, int Height,
    float MaxHP, float MoveSpeed, float DetectionRange, float AttackRange,
    float AttackDamage, float AttackCooldown, float KnockbackResistance,
    bool IsRanged, float ProjectileSpeed, LootTable Loot);
public static class EnemyRegistry { ... }
```

From Combat/EnemySpawner.cs (Plan 02):
```csharp
public class EnemySpawner
{
    public List<EnemyEntity> SpawnAll();
    public void Respawn(List<EnemyEntity> enemies);
}
```

From UI/BossHealthBar.cs (Plan 01):
```csharp
public static class BossHealthBar
{
    public static void Draw(SpriteBatch sb, Texture2D pixel, SpriteFont font,
        string bossName, float hp, float maxHp, int screenWidth, int screenHeight);
}
```

From Combat/CombatManager.cs (Plan 01):
```csharp
public class CombatManager
{
    public bool TryPlayerTakeDamage(PlayerEntity player, float rawDamage);
    public float CalculateMeleeDamage();
}
```

From Core/GameState.cs (current):
```csharp
// Needs BossKilled flag added
```

From Scenes/FarmScene.cs (after Plan 02):
```csharp
// Has _enemies list, CombatManager, ProjectileManager, EnemySpawner
// Needs boss-specific additions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BossEntity with telegraphed attacks, summon phases, and GameState integration</name>
  <files>Combat/BossEntity.cs, Combat/EnemySpawner.cs, Core/GameState.cs, Core/SaveManager.cs, Scenes/FarmScene.cs</files>
  <read_first>Combat/EnemyEntity.cs, Combat/EnemyData.cs, Combat/EnemyAI.cs, Combat/LootTable.cs, Core/GameState.cs, Core/SaveManager.cs, Scenes/FarmScene.cs, Combat/CombatManager.cs, UI/BossHealthBar.cs</read_first>
  <action>
**1. Modify Core/GameState.cs** -- Add boss tracking per D-23:
- Add property: `public bool BossKilled { get; set; } = false;`
- This flag tracks if the Skeleton King has been killed at least once, for first-kill loot guarantee.

**2. Modify Core/SaveManager.cs** -- Ensure BossKilled serializes/deserializes:
- SaveManager uses System.Text.Json to serialize GameState. Since BossKilled is a public property with get/set, it should serialize automatically. Verify by reading SaveManager to confirm no manual field mapping that would exclude it. If SaveManager manually maps fields, add BossKilled. If it serializes the whole GameState object, no change needed. Increment CURRENT_SAVE_VERSION and add migration for old saves (default BossKilled = false).

**3. Create Combat/BossEntity.cs** -- Skeleton King boss per D-21/D-22/D-23:
- Namespace: `stardew_medieval_v3.Combat`
- Extends `EnemyEntity` (inherits all AI/data-driven behavior) OR extends `Entity` directly if EnemyEntity constructor is too constrained. Preferred: extend EnemyEntity with a special boss EnemyData.
- **Boss EnemyData**: Create a static method or constant:
  - Id="SkeletonKing", Name="Skeleton King", PlaceholderColor=Color.Red, Width=32, Height=32 (2x normal per D-21), MaxHP=300, MoveSpeed=40 (between skeleton and golem), DetectionRange=200, AttackRange=40 (larger melee range), AttackDamage=25 (slash damage per D-22), AttackCooldown=3.0f, KnockbackResistance=0.5f, IsRanged=false, ProjectileSpeed=0, Loot=special (see below).
- **Summon phase tracking** per D-22 and Research Pitfall 5:
  - Field `private int _summonPhase = 0;` tracks which thresholds have triggered.
  - Thresholds: phase 1 at 70% HP (210), phase 2 at 40% HP (120). Per D-22: "spawna 2 skeletons minions a cada 30% HP perdido" -- 100%->70%=30%, 70%->40%=30%.
  - Method `List<EnemyEntity>? CheckSummonPhase()`: if HP <= 210 and _summonPhase < 1, advance to phase 1, return 2 new Skeleton EnemyEntities at positions near boss. If HP <= 120 and _summonPhase < 2, advance to phase 2, return 2 more skeletons. Otherwise return null.
- **Telegraphed attack** per D-22:
  - Field `_windUpTimer`, `_isWindingUp`, `_windUpDuration = 1.0f`.
  - Override attack logic: when AI says attack is ready, instead of immediate attack, start wind-up. During wind-up, draw boss tinted red (Color.Red * 1.5f or darker red). After wind-up completes, execute the wide slash.
  - The wide slash hitbox is larger than normal melee: ~64px wide x 32px deep (wider arc than normal enemies).
  - Method `Rectangle GetBossSlashHitbox()`: large rectangle in facing direction.
  - Property `bool IsWindingUp => _isWindingUp;`
  - Property `bool IsBossSlashReady`: true when wind-up just completed.
- **Loot** per D-23:
  - Method `List<(string itemId, int quantity)> GetBossLoot(bool bossAlreadyKilled)`:
    - Always drop: gold bonus (placeholder -- for now drop 5x Stone_Chunk as gold proxy, or skip gold since gold system is Phase 6).
    - First kill (!bossAlreadyKilled): guaranteed Flame_Blade drop. Subsequent kills: 10% chance Flame_Blade.
- Override Draw: draw larger red rectangle (32x32 per D-21: 2x sprite). During wind-up, flash between Color.Red and Color.DarkRed every 0.1s for telegraph effect.

**4. Modify Combat/EnemySpawner.cs** -- Add boss spawn:
- Add method `BossEntity SpawnBoss()`: creates BossEntity at a hardcoded position on the farm (e.g., Vector2(600, 400), away from regular enemies). Per D-21.
- Modify Respawn to also respawn boss if it was killed (or only if not BossKilled for first time -- actually per D-20 respawn on day advance, boss should respawn too for replayability).

**5. Modify Scenes/FarmScene.cs** -- Integrate boss:
- Add field: `private BossEntity? _boss;`
- In LoadContent: `_boss = _spawner.SpawnBoss();`
- In Update:
  - Update boss like other enemies: `_boss?.Update(deltaTime, _player.Position, _projectiles);`
  - Check boss wind-up and slash: if _boss.IsBossSlashReady, check slash hitbox vs player, apply 25 damage via CombatManager.TryPlayerTakeDamage. Per D-22.
  - Check boss summon phases: `var minions = _boss.CheckSummonPhase(); if (minions != null) _enemies.AddRange(minions);`
  - Apply melee hitbox checks to boss (same as regular enemies but boss has larger hitbox).
  - Handle boss death: if !_boss.IsAlive, get loot via `_boss.GetBossLoot(gameState.BossKilled)`, spawn drops, set `gameState.BossKilled = true`, set `_boss = null`.
- In Draw (world space): draw boss `_boss?.Draw(spriteBatch);` and boss enemy HP bar above it.
- In Draw (screen space / HUD section): if _boss != null and _boss.IsAlive, call `BossHealthBar.Draw(spriteBatch, _pixel, font, "Skeleton King", _boss.HP, _boss.MaxHP, screenWidth, screenHeight);` Per D-24.
- In OnDayAdvanced: respawn boss if dead `if (_boss == null || !_boss.IsAlive) _boss = _spawner.SpawnBoss();`
  </action>
  <verify>
    <automated>cd "C:/Users/tinho/OneDrive/Documentos/Github/stardew_medieval_v3" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>
- BossEntity.cs exists with Skeleton King (300 HP, 32x32 red rectangle, 2 attack patterns)
- Wind-up telegraph: 1s red flash before slash attack
- Summon phases at 70% HP and 40% HP, each spawning 2 skeleton minions
- First-kill drops guaranteed Flame_Blade per D-23
- GameState.BossKilled persists across save/load
- BossHealthBar renders at screen bottom when boss is alive
- Boss respawns on day advance
- dotnet build succeeds with 0 errors
  </done>
  <acceptance_criteria>
- `grep -c "class BossEntity" Combat/BossEntity.cs` returns 1
- `grep -c "SkeletonKing\|Skeleton King" Combat/BossEntity.cs` returns >= 1
- `grep "300" Combat/BossEntity.cs` returns match (boss HP)
- `grep -c "_summonPhase\|SummonPhase\|CheckSummon" Combat/BossEntity.cs` returns >= 2
- `grep -c "_windUpTimer\|WindUp\|windUp" Combat/BossEntity.cs` returns >= 2
- `grep -c "Flame_Blade" Combat/BossEntity.cs` returns >= 1
- `grep -c "BossKilled" Core/GameState.cs` returns >= 1
- `grep -c "_boss" Scenes/FarmScene.cs` returns >= 4
- `grep -c "BossHealthBar" Scenes/FarmScene.cs` returns >= 1
- `dotnet build` exits with code 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete combat system</name>
  <files>none</files>
  <action>
Human gameplay verification of the complete Phase 03 combat system. Run the game and test all 6 ROADMAP success criteria (CMB-01 through CMB-06). Steps:
1. Run `dotnet run` from project root
2. Equip a weapon: Open inventory (I), place Iron_Sword in hotbar, select with number key
3. Test melee (CMB-01): Walk toward white Skeleton, press LMB. Check slash visual, damage, knockback, white flash.
4. Test fireball (CMB-02): Press RMB. Check orange projectile travels, damages on hit, 2s cooldown on HUD.
5. Test HP bars (CMB-03): Check player HP bar in HUD, enemy HP bars above damaged enemies.
6. Test enemy types (CMB-04): Skeleton rushes, Mage keeps distance and shoots purple, Golem slow and tanky.
7. Test AI (CMB-05): Walk away from enemies beyond detection range, check they return to spawn.
8. Test i-frames: Let enemy hit you, check blink for 1s, immune during blink.
9. Test boss (CMB-06): Fight Skeleton King (large red). Check bottom HP bar, red flash telegraph, skeleton summons at 70% and 40% HP, Flame_Blade drop on kill.
10. Test death: Let enemies kill you, check respawn at farm with full HP.
11. Test day respawn: Sleep to advance day, check all enemies and boss respawn.
  </action>
  <verify>
    <automated>cd "C:/Users/tinho/OneDrive/Documentos/Github/stardew_medieval_v3" && dotnet build 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 6 ROADMAP success criteria for Phase 3 are met</done>
  <acceptance_criteria>
- `dotnet build` exits with code 0
- Human confirms melee attack damages enemies with visible knockback
- Human confirms fireball travels and damages on hit
- Human confirms HP bars visible for player, enemies, and boss
- Human confirms 3 enemy types behave differently
- Human confirms boss telegraphs attacks and summons minions
- Human confirms boss drops Flame_Blade on first kill
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- single-player local game with no network, auth, or external data.

## STRIDE Threat Register

No threats identified. Single-player offline game with local save files only.
</threat_model>

<verification>
After all tasks complete:
1. `dotnet build` succeeds with 0 errors
2. Boss spawns as large red rectangle with 300 HP
3. Boss telegraphs attacks with red flash
4. Boss summons minions at HP thresholds
5. First boss kill drops Flame_Blade
6. BossKilled flag persists in save file
7. Human verification confirms full combat experience
</verification>

<success_criteria>
- Skeleton King boss functions with two attack patterns (telegraphed slash + minion summon)
- Boss HP bar renders at screen bottom with name
- First-kill loot (Flame_Blade) drops correctly
- BossKilled persists across save/load
- Human player confirms combat is satisfying and all 6 success criteria from ROADMAP are met
</success_criteria>

<output>
After completion, create `.planning/phases/03-combat/03-03-SUMMARY.md`
</output>
