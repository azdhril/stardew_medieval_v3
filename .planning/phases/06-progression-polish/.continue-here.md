---
context: phase
phase: 06-progression-polish
task: discuss-complete
total_tasks: 3
status: paused
last_updated: 2026-04-16T21:58:05.571Z
---

<current_state>
Phase 6 discuss-phase is complete. `06-CONTEXT.md` (24 locked decisions) and `06-DISCUSSION-LOG.md` are written and committed as `4bc7d21`. Session was paused at 91% context right after the commit — before STATE.md could be updated or the auto-advance to plan-phase could happen.

Next logical step is `/gsd-plan-phase 6` which will read `06-CONTEXT.md` and kick off research + plan generation.
</current_state>

<completed_work>

Completed:
- Phase 6 discussion across 4 gray areas (XP/leveling + level-up feedback, Gold drops, Death penalty, HUD polish + save scope)
- `06-CONTEXT.md` with 24 decisions (D-01..D-24), canonical refs to code touchpoints and prior phase CONTEXT.md files, code_context insights, specifics, deferred ideas
- `06-DISCUSSION-LOG.md` audit trail (options presented, user selections, free-text notes)
- Commit `4bc7d21` — docs(06): capture phase context
</completed_work>

<remaining_work>

- Run `/gsd-plan-phase 6` to generate RESEARCH.md + phase plans
- Execute the plans (XP/leveling system, gold drops, death penalty, HUD polish, periodic save + v9 migration)
- Phase 6 verification/review/roadmap update
</remaining_work>

<decisions_made>

**XP & Leveling (D-01..D-08):**
- Tier-flat XP: Skeleton 10, Dark Mage 15, Golem 25, Skeleton King 150
- Level cap = 100 (narrative ceiling); curve makes boss beatable at level ~8-15
- +10 HP / +1 dmg / +5 stamina per level
- Level-up = "LEVEL UP! Lv X" banner (~1.5s) + gold particle burst, no pause, no SFX

**Gold (D-09..D-12):**
- Coin = new `Gold_Coin` ItemDropEntity with magnetism (reuses Phase 2 pickup)
- 5g/8g/15g per tier + 100g boss, ±30% variance, every kill guaranteed

**Death penalty (D-13..D-17):**
- ALL deaths penalize (farm + dungeon), dungeon reset still happens
- -10% gold always (floor, never negative)
- Item loss: 60% nothing / 25% lose 1 / 15% lose 2, pool = ANY slot (inventory + hotbar + equipment)
- Respawn = farm center (10,10), full HP/stamina
- UX: red "You died" banner + Toast list per lost item

**HUD (D-18..D-19):**
- Graphical upgrades: XP bar above hotbar + Lv label, NineSlice clock panel, gold icon panel, quest tracker with icon + NineSlice
- ALSO fold in: clean up ugly HP/MP/STA in-bar text (Claude's Discretion: remove / shrink / tooltip / mini-panel)
- Reuse `UI_Progress_Style1_*`, `UI_Icon_Sys_Gold.png`, existing NineSlice/UITheme

**Save (D-20..D-24):**
- Periodic auto-save every ~30s gameplay real-time + level-up + post-death
- Existing triggers (day-advance, shop close, boss victory, pre-death, F5) preserved
- Save version v8 → v9: persist MaxHP / MaxStamina / BaseDamageBonus (derived from Level for legacy saves)
- Perf guard (D-21): if >50ms, planner adds write-on-dirty or async
- Respawn location stays fixed at farm center
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-progression-polish/06-CONTEXT.md` — ALL 24 phase decisions + canonical refs
2. `.planning/phases/06-progression-polish/06-DISCUSSION-LOG.md` — alternatives considered
3. `.planning/ROADMAP.md` §"Phase 6: Progression & Polish" — success criteria + requirements
4. `.planning/REQUIREMENTS.md` — PRG-01..04, HUD-01, HUD-04, SAV-01, SAV-02
5. Prior phase CONTEXT.md files listed in `<canonical_refs>` of 06-CONTEXT.md (especially Phase 3 death contract and Phase 5 dungeon reset contract)

## Critical Anti-Patterns (do NOT repeat these)
- None discovered this session.

## Infrastructure State
- Git: on `master`, clean working tree after commit `4bc7d21`
- `.planning/STATE.md`: **STALE** — still shows "Executing Phase 05" / "Completed 05-04-PLAN.md". Phase 5 is actually complete (commit `1651965`), Phase 6 discuss is done. Next `/gsd-plan-phase 6` init should refresh.
- No background processes running
- No uncommitted files

<context>
Discussion ran through all 4 selected gray areas in one pass. User was decisive on most choices (often picked Recommended) and gave free-text clarifications on three: (1) level cap = 100 as narrative ceiling, (2) death penalty item loss bucket 60/25/15 across any slot, (3) periodic auto-save "like a normal game". Those nuances are captured verbatim in decisions D-02/D-03, D-15, and D-20/D-21.

Context pressure hit 91% right after the docs commit, so STATE.md update and auto-advance were skipped. This is safe — STATE.md position is derivable from roadmap + commit history, and `/gsd-plan-phase 6` will re-init.

Key existing infrastructure to reuse (don't re-invent during planning):
- `GameState.XP/Level/Gold` fields already exist (since save v3) — just need logic
- `InventoryManager.AddGold/TrySpendGold/OnGoldChanged` complete Gold API
- `GameStateSnapshot.SaveNow()` centralized save hook — periodic timer just calls this
- `SaveManager.MigrateIfNeeded` migration chain — follow v3→v4→...→v8 pattern for v8→v9
- ItemDropEntity + magnetism from Phase 2 — coin reuses directly
- `Toast.cs` for death UX messages
- `NineSlice` + `UITheme` for all new panels
- `UI_Progress_Style1_*` sprites for XP bar (no new assets needed)
- Death check `if (!Player.IsAlive)` pattern already present at `FarmScene:378` and `DungeonScene:298` — penalty + save hooks in before the respawn

LootTable today only drops items by ID — needs extension (GoldMin/GoldMax field OR a special `Gold_Coin` LootDrop entry). Planner decides.
</context>

<next_action>
Run `/gsd-plan-phase 6` to kick off research + plan generation against `06-CONTEXT.md`. No manual setup needed — context is complete, no blockers, no pending human actions.
</next_action>
