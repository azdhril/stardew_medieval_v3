---
phase: 04.1-shop-save-gap-closure
verified: 2026-04-13T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  signed_off: 2026-04-17
  signed_off_by: user (Renato Guedes)
  note: "All 6 human_verification tests confirmed in-game: scroll persistence (no snap-back), per-row qty independence, mouse-only enforcement with [ShopPanel] logs, shop buy Gold+Inventory round-trip after relaunch, F5 manual save of quest state, and Farm sleep (P) save regression check. Scroll regression and save-persistence gaps from Phase 04 are closed."
human_verification:
  - test: "Open shop with >8 buy items; roll wheel down; confirm last row is visible and stays visible across frames (no snap-back)"
    expected: "List scrolls to bottom and stays there. No rubber-band back to top."
    why_human: "Cannot observe frame-by-frame scroll persistence without running the game."
  - test: "Click + on row 3 three times; confirm row 3 shows qty=4, rows 1/2/4 still show qty=1. Click Buy on row 3."
    expected: "Row 3 purchases 4 units. Row 3 qty resets to 1. Other rows unchanged."
    why_human: "Per-row qty independence requires visual confirmation inside the running game."
  - test: "Click outside panel; click X button; switch tabs; press Up/Down/Enter. Press Escape."
    expected: "Outside/X close with [ShopPanel] logs. Tab switch logs. Arrow keys do nothing. Escape closes."
    why_human: "Mouse-only enforcement and log emission require live game execution."
  - test: "Walk to shop, buy item, press Esc (overlay close). Quit and relaunch. Inspect Gold and Inventory."
    expected: "Gold reduced by purchase price and Inventory contains purchased item after relaunch."
    why_human: "Save round-trip verification (Gold + Inventory persistence) requires running the game and restarting."
  - test: "Talk to King (advance QuestState). Press F5. Quit and relaunch. Check quest tracker HUD."
    expected: "Quest state advanced (QuestState >= 1) after relaunch. [VillageScene] F5 manual save log visible."
    why_human: "Quest persistence through F5 requires game execution across relaunch."
  - test: "Press P in Farm (sleep). Relaunch. Confirm DayNumber incremented and FarmCells match pre-sleep state."
    expected: "Sleep save regression not introduced — Farm sleep still persists state correctly."
    why_human: "FarmScene.OnDayAdvanced refactor correctness requires live game verification."
---

# Phase 04.1: Shop UX & Save Gap Closure — Verification Report

**Phase Goal:** Close three UAT gaps from Phase 04: (1) save persistence — Gold/quest/inventory round-trip through SaveManager (HUD-05); (2) full mouse navigation of the shop (NPC-03, NPC-04); (3) scroll-wheel regression fix in ShopPanel (NPC-03, NPC-04).
**Verified:** 2026-04-13
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `_scrollOffset` is only modified by wheel input and range-clamp; no follow-selection block | ✓ VERIFIED | `grep -c "_scrollOffset = _selectedIndex" src/UI/ShopPanel.cs` = 0; "Wheel-driven only" comment at line 203; no `_selectedIndex` in Update hit-test |
| 2 | Rolling wheel scrolls the list and does NOT snap back on next frame | ? HUMAN | Code path confirmed correct (only write sites are wheel handler line 87 and clamps); observable behavior requires running game |
| 3 | Every visible row shows its own `-`/qty/`+` and Buy/Sell controls | ✓ VERIFIED | `_qtyMinusRects[]`, `_qtyLabelRects[]`, `_qtyPlusRects[]` declared (lines 55-57); layout populated in `UpdateLayoutCache` per-row loop (lines 231-233); `DrawRow` renders per-row stepper at lines 594-621 |
| 4 | Keyboard navigation removed except Escape; all click paths log `[ShopPanel]` | ✓ VERIFIED | `grep Keys.(Up\|Down\|Left\|Right\|Tab\|Enter)` = 0; `Keys.Escape` = 1; all 10 log strings present (Close X, outside, Tab Buy/Sell, qty-/qty+, Buy-click/Sell-click, action-disabled, Escape) |
| 5 | `_rowQty[]` is per-row and independent; `EnsureRowQtySized` resets on tab switch | ✓ VERIFIED | `private int[] _rowQty` declared (line 50); `EnsureRowQtySized` at line 289 checks `_rowQtyTab == _tab`; tab-switch path calls `EnsureRowQtySized(GetRowCount())` at lines 117, 127 |
| 6 | `GameStateSnapshot.SaveNow` is wired to FarmScene sleep, ShopOverlayScene close, and GameplayScene F5 | ✓ VERIFIED | `FarmScene.cs:574`, `ShopOverlayScene.cs:45`, `GameplayScene.cs:114-118`; `SaveManager.Save(state)` removed from FarmScene (0 occurrences) |
| 7 | Gold/quest/inventory round-trip persists after relaunch | ? HUMAN | Code path correct: `SaveNow` calls `services.Inventory?.SaveToState(state)` and `services.Quest?.SaveToState(state)` before `SaveManager.Save(state)` (GameStateSnapshot.cs lines 37-44); observable round-trip requires running game + relaunch |

**Score:** 7/7 truths verified (5 programmatically confirmed; 2 require human runtime validation)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/UI/ShopPanel.cs` | Wheel-only scroll; per-row stepper; mouse-only input; full click logging | ✓ VERIFIED | File exists, 657 lines, substantive. Follow-selection block deleted, `_rowQty[]` present, `EnsureRowQtySized` present, `GetMaxQty` present, keyboard nav removed, all 10 log strings present. |
| `src/Core/GameStateSnapshot.cs` | `public static class GameStateSnapshot` with `SaveNow` entry point | ✓ VERIFIED | File exists (46 lines). `public static class GameStateSnapshot` at line 12. `public static void SaveNow(ServiceContainer services, List<FarmCellSaveData>? farmCells = null)` at line 21. |
| `src/Scenes/FarmScene.cs` | OnDayAdvanced uses `GameStateSnapshot.SaveNow(...)` | ✓ VERIFIED | `GameStateSnapshot.SaveNow(Services, _gridManager.GetSaveData())` at line 574; `SaveManager.Save(state)` count = 0 (fully replaced) |
| `src/Scenes/ShopOverlayScene.cs` | SaveNow call on overlay close | ✓ VERIFIED | `[ShopOverlayScene] Closed -> SaveNow` log at line 44; `GameStateSnapshot.SaveNow(Services)` at line 45; before `PopImmediate()` at line 46 |
| `src/Core/GameplayScene.cs` | F5 manual save hotkey in base gameplay class | ✓ VERIFIED | `if (input.IsKeyPressed(Keys.F5))` at line 114; `GameStateSnapshot.SaveNow(Services)` at line 116; log `$"[{SceneName}Scene] F5 manual save"` at line 117 |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `input.ScrollWheelDelta` | `_scrollOffset` | wheel handler line 87 | ✓ WIRED | `_scrollOffset = Math.Clamp(_scrollOffset - Math.Sign(wheel), 0, maxScroll)` |
| `UpdateLayoutCache` | `_scrollOffset` clamp only | range-clamp lines 200-201 | ✓ WIRED | No follow-selection; only clamp present |
| `input.IsLeftClickPressed + MousePosition` | `_qtyMinusRects[k]/_qtyPlusRects[k]/_actionBtnRects[k]` | `Contains(mp)` hit-test loop lines 134-175 | ✓ WIRED | Per-visible-row hit-test with `abs = k + _scrollOffset` |
| Tab switch + row mutation | `_rowQty` reset | `EnsureRowQtySized` at lines 117, 127, 380, 424 | ✓ WIRED | Called on tab change (lines 117, 127), after Buy (line 380), after Sell (line 424) |
| `GameStateSnapshot.SaveNow(Services)` | `SaveManager.Save(state)` | Build GameState from services (lines 23-44) | ✓ WIRED | `services.Inventory?.SaveToState(state)` + `services.Quest?.SaveToState(state)` → `SaveManager.Save(state)` |
| `ShopOverlayScene.Update` close path | `GameStateSnapshot.SaveNow` | before `PopImmediate` (lines 44-46) | ✓ WIRED | Save fires before scene pop |
| F5 keypress in `GameplayScene.Update` | `GameStateSnapshot.SaveNow` | `input.IsKeyPressed(Keys.F5)` at line 114 | ✓ WIRED | At top of shared Update, before Escape/inventory/movement |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `GameStateSnapshot.cs` | `state.Gold`, `state.Inventory`, `state.QuestState` | `services.Inventory.SaveToState(state)`, `services.Quest.SaveToState(state)` | Yes — delegates to `InventoryManager.SaveToState` and `MainQuest.SaveToState` which write live state | ✓ FLOWING |
| `ShopPanel.cs` `_rowQty[]` | per-row qty for display | `EnsureRowQtySized` initializes to 1; incremented/decremented by user clicks; bounded by `GetMaxQty` | Yes — `GetMaxQty` reads `_inv.Gold` and stack quantities from live inventory | ✓ FLOWING |

---

## Behavioral Spot-Checks

Step 7b: SKIPPED — all observable behaviors require running game window (MonoGame DesktopGL, no headless execution path). Build verification serves as the programmatic gate.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build compiles clean | `dotnet build -c Debug --nologo -v quiet` | exit 0, 0 errors, 1 pre-existing CS8602 warning (GameplayScene.cs:177, unrelated) | ✓ PASS |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| NPC-03 | 04.1-01, 04.1-02 | Shopkeeper NPC com UI de compra/venda (sementes, pocoes, equipamento basico) | ✓ SATISFIED | ShopPanel rewritten: wheel scroll fixed, per-row stepper implemented, mouse-only input, full click logging |
| NPC-04 | 04.1-01, 04.1-02 | NPCs com estado de quest que altera dialogo | ✓ SATISFIED | Quest state now persists via SaveNow; shop UX improvements complete |
| HUD-05 | 04.1-03 | Caixa de dialogo estilizada / shop UX completeness + save persistence | ✓ SATISFIED | `GameStateSnapshot.SaveNow` wired into shop close, F5, and Farm sleep; Gold/Quest/Inventory all flow through SaveToState calls |

Note: REQUIREMENTS.md marks all three as `Complete` at Phase 4, which this phase closes programmatically. The save round-trip behavioral verification (Gold/Inventory/Quest after relaunch) remains for human validation.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | All prior stubs removed | — | — |

Scanned for: TODO/FIXME, `return null`/`return {}`/`return []`, hardcoded empty initial state flowing to render. No blockers. `_rowQty = Array.Empty<int>()` at line 50 is an initial field declaration overwritten by `EnsureRowQtySized` before any hit-test — not a stub.

---

## Human Verification Required

### 1. Wheel Scroll Persistence

**Test:** Open shop with more than 8 buy items; scroll wheel to last row; wait 2-3 seconds without input.
**Expected:** List stays at the scrolled position — no rubber-band snap back to top.
**Why human:** Frame-by-frame scroll stability requires live rendering; grep confirms no follow-selection code exists but cannot substitute for observable behavior.

### 2. Per-Row Qty Independence

**Test:** Click `+` on row 3 three times. Observe row 3 qty = 4. Verify rows 1, 2, 4 still show qty = 1. Click Buy on row 3.
**Expected:** Transaction buys 4 units for row 3 only. Row 3 qty resets to 1. Other rows unchanged.
**Why human:** Array indexing correctness requires visual confirmation in running game.

### 3. Mouse-Only Input Enforcement

**Test:** Open shop. Press Up, Down, Enter, Tab (keyboard). Verify nothing happens. Press Esc.
**Expected:** Arrow/Tab/Enter keys have no effect inside the shop panel. Escape closes.
**Why human:** Input suppression requires running game — grep confirms keyboard keys are absent from source, but the game must be exercised to confirm no other key handlers intercept.

### 4. Shop Close → Save Round-Trip (Gold + Inventory)

**Test:** Launch game → buy 1 item from shop → press Esc to close overlay. Check console for `[ShopOverlayScene] Closed -> SaveNow` and `[GameStateSnapshot] SaveNow: gold=<reduced>`. Quit game. Relaunch. Inspect Gold and Inventory.
**Expected:** Gold reduced by purchase price; purchased item present in inventory after relaunch.
**Why human:** Save persistence requires game execution across process restart.

### 5. F5 Save → Quest State Round-Trip

**Test:** Talk to King (advance QuestState). Press F5. Check console for `[VillageScene] F5 manual save` and `[GameStateSnapshot] SaveNow: quest=1`. Quit game. Relaunch.
**Expected:** Quest state shows advanced state after relaunch. HUD quest tracker reflects `quest=1`.
**Why human:** Quest persistence requires game execution across process restart.

### 6. Farm Sleep Regression

**Test:** Press P in Farm. Check console for `[GameStateSnapshot] SaveNow: day=<N+1>`. Quit and relaunch.
**Expected:** DayNumber incremented, FarmCells preserved, stamina reset — no regression from FarmScene.OnDayAdvanced refactor.
**Why human:** Save refactor correctness requires live game verification.

---

## Gaps Summary

No programmatic gaps found. All must-have code paths exist, are substantive, and are correctly wired:

- D-01/D-02: Follow-selection block deleted from `UpdateLayoutCache`; `_scrollOffset` written only by wheel handler (line 87) and two range-clamps (lines 200, 421). Zero occurrences of `_scrollOffset = _selectedIndex`.
- D-03: Keyboard navigation fully removed from `ShopPanel.Update`; only `Keys.Escape` remains (1 occurrence).
- D-04: `_rowQty[]` per-row array with `EnsureRowQtySized` and `GetMaxQty`; layout computes `_qtyMinusRects[]/_qtyLabelRects[]/_qtyPlusRects[]` per visible row; `DrawRow` renders per-row stepper.
- D-05: All click paths emit `[ShopPanel]` logs; tabs and close are mouse-only.
- D-06: `GameStateSnapshot.SaveNow` wired into all three pinch points; `SaveManager.Save(state)` removed from FarmScene inline construction.
- Build: `dotnet build` exits 0, 0 errors, 1 pre-existing warning only.

Status is `human_needed` because 6 behavioral tests require a running game instance to confirm round-trip persistence and scroll/UX behavior.

---

_Verified: 2026-04-13_
_Verifier: Claude (gsd-verifier)_
