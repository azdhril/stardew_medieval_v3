---
phase: quick-260415-uq9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/UI/HUD.cs
  - src/UI/HotbarRenderer.cs
autonomous: false
requirements:
  - QUICK-UQ9-01  # sprite-based HP + Stamina bars
  - QUICK-UQ9-02  # decorative slot-pane panel behind hotbar
must_haves:
  truths:
    - "HP bar top-left renders with UI_StatusBar_Bg as frame and UI_StatusBar_Fill_HP as the fill, not flat rectangles"
    - "Stamina bar bottom-left renders with UI_StatusBar_Bg frame and UI_StatusBar_Fill_Green (or Yellow) fill, not flat rectangles"
    - "Fill bars shrink left-to-right via source-rect crop (no horizontal stretch/squash of the fill sprite) as HP/Stamina decrease"
    - "A UI_Panel_SlotPane sprite is drawn behind the main hotbar slots (centered under the 8 slots at the bottom), and the slot sprites render ON TOP of it unchanged"
    - "No gameplay behavior changes: HP/Stamina values, cooldown indicator, quest tracker, hotbar drag/drop still work"
    - "Sprites load via Texture2D.FromStream from assets/ paths (matches existing HotbarRenderer/FarmScene pattern — no Content.mgcb edits)"
    - "If any sprite fails to load, HUD logs [HUD] with the error and falls back to the previous flat-rect rendering (fail-secure for UI: never crash the game)"
  artifacts:
    - path: "src/UI/HUD.cs"
      provides: "Sprite-based HP + Stamina bar rendering with source-rect fill crop"
      contains: "UI_StatusBar_Bg, UI_StatusBar_Fill_HP, UI_StatusBar_Fill_Green"
    - path: "src/UI/HotbarRenderer.cs"
      provides: "UI_Panel_SlotPane texture loaded and drawn behind the main hotbar slots"
      contains: "UI_Panel_SlotPane"
  key_links:
    - from: "src/UI/HUD.cs Draw()"
      to: "assets/Sprites/System/UI Elements/Bars/Status/*.png"
      via: "Texture2D.FromStream loaded once in LoadContent, drawn with source-rect crop each frame"
      pattern: "UI_StatusBar_Fill_HP"
    - from: "src/UI/HotbarRenderer.cs Draw()"
      to: "assets/Sprites/System/UI Elements/Panel/UI_Panel_SlotPane.png"
      via: "Texture2D.FromStream loaded in LoadContent, drawn before slot loop (behind slots)"
      pattern: "UI_Panel_SlotPane"
---

<objective>
Replace the flat-color rectangle HP and Stamina bars in the HUD with sprite-based
rendering using the UI_StatusBar_* assets, and draw a UI_Panel_SlotPane decorative
panel behind the main hotbar slots.

Purpose: Ship the visual polish upgrade shown in the user's mockup without touching
any gameplay logic. Pure render swap.

Output: HP bar (top-left) and Stamina bar (bottom-left) render as pixel-art sprites
with source-rect-cropped fills; hotbar has a decorative panel behind the 8 slots.
</objective>

<context>
@./CLAUDE.md
@.planning/STATE.md

Current HUD flat-rect bars: src/UI/HUD.cs lines 51-85
  - HP bar: x=12, y=32, w=104, h=12 (per D-12)
  - Stamina bar: x=12, y=screenHeight-30, w=120, h=16

Current hotbar layout: src/UI/HotbarRenderer.cs
  - SlotSize=42, 8 main slots, centered horizontally, BottomMargin=8
  - GetMainSlotRect(i, screenWidth, screenHeight) returns slot rects
  - First slot X = (screenWidth - hotbarWidth) / 2, Y = screenHeight - 42 - 8

Existing texture-load pattern (MUST MATCH — do NOT add to Content.mgcb):
  using var stream = File.OpenRead("assets/Sprites/.../Foo.png");
  _tex = Texture2D.FromStream(device, stream);
  See HotbarRenderer.LoadContent lines 75-108 for the exact try/catch idiom
  (log "[Module] Failed to load ...: {ex.Message}" and fall back to a 1x1 pixel).

Player HP/MaxHP already exist on PlayerEntity (via src/Core/Entity.cs:19-20).
HUD already reads _player.HP / _player.MaxHP — no PlayerStats changes needed.

Asset paths (all already on disk — verified):
  assets/Sprites/System/UI Elements/Bars/Status/UI_StatusBar_Bg.png
  assets/Sprites/System/UI Elements/Bars/Status/UI_StatusBar_Fill_HP.png
  assets/Sprites/System/UI Elements/Bars/Status/UI_StatusBar_Fill_Green.png
  assets/Sprites/System/UI Elements/Panel/UI_Panel_SlotPane.png

Fill-crop technique (pixel art — no stretching):
  float pct = current / max;   // 0..1
  int fillW  = (int)(fillTex.Width * pct);
  var srcRect  = new Rectangle(0, 0, fillW, fillTex.Height);
  var destRect = new Rectangle(barX, barY, fillW, fillTex.Height);
  sb.Draw(fillTex, destRect, srcRect, Color.White);

Bar dest sizing: draw the Bg sprite at its native pixel size (sb.Draw(tex, new Vector2(x,y), Color.White)) so the art is never stretched. Fills are cropped from the left at native size. Bar positions should remain approximately in the same corners (top-left for HP, bottom-left for Stamina) — adjust (x,y) to the sprite's native dimensions so text labels still fit visibly alongside/over the bar.

Panel backdrop: draw UI_Panel_SlotPane centered behind the 8 main hotbar slots BEFORE the slot loop. Compute panel rect from GetMainSlotRect(0, ...) and GetMainSlotRect(7, ...) to span all 8 slots, with a small padding (4-8px) on each side and above/below. Panel should NOT intercept clicks (it's purely decorative — drawn before slots which handle input).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load status-bar sprites and swap HP + Stamina rendering in HUD.cs</name>
  <files>src/UI/HUD.cs</files>
  <action>
Add three Texture2D? fields to HUD: _barBg, _barFillHP, _barFillStamina.

In HUD.LoadContent(device, font), after creating _pixel, load the three PNGs using
the exact try/catch idiom from HotbarRenderer.LoadContent (File.OpenRead +
Texture2D.FromStream, log "[HUD] Failed to load {name}: {ex.Message}" on failure and
leave the field as null — DO NOT throw). Use these paths:

  assets/Sprites/System/UI Elements/Bars/Status/UI_StatusBar_Bg.png          -> _barBg
  assets/Sprites/System/UI Elements/Bars/Status/UI_StatusBar_Fill_HP.png     -> _barFillHP
  assets/Sprites/System/UI Elements/Bars/Status/UI_StatusBar_Fill_Green.png  -> _barFillStamina

Extract a private helper:
  private void DrawSpriteBar(SpriteBatch sb, int x, int y, Texture2D? bg, Texture2D? fill, float pct)
Behavior:
  - If bg == null OR fill == null: return false (caller uses flat-rect fallback).
  - Otherwise: sb.Draw(bg, new Vector2(x, y), Color.White);
              int fillW = (int)MathHelper.Clamp(fill.Width * pct, 0, fill.Width);
              if (fillW > 0) {
                var src  = new Rectangle(0, 0, fillW, fill.Height);
                var dest = new Rectangle(x, y, fillW, fill.Height);
                sb.Draw(fill, dest, src, Color.White);
              }
  - Return true.

In Draw():
  - HP bar block (currently lines ~56-67): replace the two DrawRect background calls
    and the DrawRect fill call with DrawSpriteBar(spriteBatch, 12, 32, _barBg,
    _barFillHP, hpFill). If DrawSpriteBar returns false, keep the existing flat-rect
    code as fallback. KEEP the HP label DrawString call — position the text so it
    sits just above or to the right of the sprite bar (use the sprite's Height to
    compute the text Y; fall back to current y=31 if _barBg is null). No logic change
    to hpFill calculation.
  - Stamina bar block (currently lines ~69-85): same treatment. Call
    DrawSpriteBar(spriteBatch, 12, screenHeight - _barBg?.Height - 14 ?? (screenHeight - 30),
    _barBg, _barFillStamina, fill). KEEP the STA label DrawString. REMOVE the
    stamina-color-by-percent ternary (the green fill sprite IS the color now); if
    sprites fail to load, the fallback flat-rect path keeps the old behavior.

DO NOT touch: DrawQuestTracker, DrawFireballCooldown, the _quest/SetQuest API, or any
non-bar rendering. DO NOT add new using directives beyond Microsoft.Xna.Framework
(already imported).

C# 12 conventions: _barBg / _barFillHP / _barFillStamina declared as
`private Texture2D? _barBg;` (nullable reference types per .csproj `<Nullable>enable</Nullable>`).
  </action>
  <verify>
    <automated>dotnet build stardew_medieval_v3.csproj /p:WarningLevel=4</automated>
  </verify>
  <done>
    - Build succeeds with zero new warnings.
    - Running the game shows sprite-rendered HP bar top-left and Stamina bar
      bottom-left with fills that shrink left-to-right as values decrease.
    - If one of the 3 PNGs is temporarily renamed to simulate a missing asset, game
      still starts and falls back to flat-rect rendering with a "[HUD] Failed to load"
      console line (fail-secure).
  </done>
</task>

<task type="auto">
  <name>Task 2: Draw UI_Panel_SlotPane behind main hotbar slots</name>
  <files>src/UI/HotbarRenderer.cs</files>
  <action>
Add a field: `private Texture2D? _panelSlotPane;`

In HotbarRenderer.LoadContent(device, font), after the existing _slotSelected and
_handIcon loads, add a fourth try/catch block that loads
  assets/Sprites/System/UI Elements/Panel/UI_Panel_SlotPane.png
into _panelSlotPane using the exact same try/catch idiom (log
"[HotbarRenderer] Failed to load UI_Panel_SlotPane: {ex.Message}" on failure, leave
the field null — DO NOT throw and DO NOT substitute a 1x1 fallback; null means "no
panel", which is the desired graceful fallback).

In Draw(), BEFORE the "Draw main hotbar" loop (line ~229), add:

  if (_panelSlotPane != null)
  {
      var firstSlot = GetMainSlotRect(0, screenWidth, screenHeight);
      var lastSlot  = GetMainSlotRect(InventoryManager.HotbarSize - 1, screenWidth, screenHeight);
      const int PadX = 6;
      const int PadY = 6;
      var panelRect = new Rectangle(
          firstSlot.X - PadX,
          firstSlot.Y - PadY,
          (lastSlot.Right - firstSlot.X) + PadX * 2,
          firstSlot.Height + PadY * 2);
      sb.Draw(_panelSlotPane, panelRect, Color.White);
  }

IMPORTANT: the panel must be drawn AFTER the consumable-slot loop (so it only sits
behind the 8 main slots, not the Q consumable) and BEFORE the main hotbar loop (so
slots render on top of it). Place the new block exactly between those two existing
loops.

DO NOT change: SlotSize, layout math, hit-testing, drag/drop logic, consumable slot
rendering, or the dragged-item overlay. DO NOT draw the panel behind the consumable
slot.
  </action>
  <verify>
    <automated>dotnet build stardew_medieval_v3.csproj /p:WarningLevel=4</automated>
  </verify>
  <done>
    - Build succeeds with zero new warnings.
    - Running the game shows the UI_Panel_SlotPane sprite centered behind the 8 main
      hotbar slots at the bottom of the screen. Consumable (Q) slot is unchanged
      (no panel behind it). All slot sprites render ON TOP of the panel.
    - Hotbar drag-and-drop, number-key selection (1-8), and click-to-select still
      work unchanged.
    - If UI_Panel_SlotPane.png is temporarily renamed, game starts normally, hotbar
      renders without any panel, and console logs
      "[HotbarRenderer] Failed to load UI_Panel_SlotPane".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies HUD sprite swap looks correct in-game</name>
  <what-built>
    - HP bar (top-left) now rendered from UI_StatusBar_Bg + UI_StatusBar_Fill_HP sprites.
    - Stamina bar (bottom-left) now rendered from UI_StatusBar_Bg + UI_StatusBar_Fill_Green sprites.
    - Fills crop left-to-right via source-rect (no stretch).
    - UI_Panel_SlotPane decorative panel renders behind the 8 main hotbar slots.
    - Flat-rect fallback still works if any sprite fails to load.
  </what-built>
  <how-to-verify>
    1. Run `dotnet run` from the repo root.
    2. Observe top-left HUD: HP bar should be a pixel-art bar (not a flat red
       rectangle). Label "HP: 100/100" still readable.
    3. Observe bottom-left HUD: Stamina bar should be a pixel-art bar (not a flat
       green/yellow/red rectangle). Label "STA: ..." still readable.
    4. Take damage (walk into enemy in dungeon) OR use stamina (till soil in farm).
       The fill portion of each bar should SHRINK from the right edge toward the
       left — the fill end should look crisp pixel art, NOT a stretched/squashed
       version of the full fill.
    5. Look at bottom-center hotbar: there should be a decorative panel visible
       BEHIND the 8 slots. Slot sprites should render cleanly on top of it. The
       Q consumable slot on the left should NOT have a panel behind it.
    6. Press 1-8 to switch active slot; drag an item between two hotbar slots.
       Everything should still work as before.
    7. Optional fail-secure check: temporarily rename
       UI_StatusBar_Bg.png -> UI_StatusBar_Bg.png.bak, relaunch. Game must still
       start; HUD falls back to flat rectangles; console shows "[HUD] Failed to
       load". Restore the file afterward.
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 visual checks pass, or describe any issues (wrong position, stretched fill, panel in wrong place, etc.)</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with no new warnings.
- Visual inspection in-game confirms sprite rendering, crop-not-stretch fill behavior, panel placement, and that gameplay behavior is unchanged.
- Fail-secure: removing any of the 4 new PNGs causes a logged warning but not a crash; game falls back to prior flat-rect rendering for bars and to no-panel for the hotbar.
</verification>

<success_criteria>
- HP and Stamina bars render from the 3 status-bar sprites instead of DrawRect primitives.
- Fills use source-rect crop (pixel-perfect, no stretching).
- UI_Panel_SlotPane renders behind the 8 main hotbar slots (not behind Q slot).
- Zero changes to PlayerStats, InventoryManager, gameplay logic, or Content.mgcb.
- All existing HUD features (quest tracker, fireball cooldown, day/time label) still work.
- Human-verify checkpoint passes.
</success_criteria>

<output>
After completion, create `.planning/quick/260415-uq9-substituir-barras-de-hp-e-stamina-planas/260415-uq9-SUMMARY.md` summarizing:
- Files changed (src/UI/HUD.cs, src/UI/HotbarRenderer.cs)
- Sprites wired (4 PNGs, loaded via Texture2D.FromStream)
- Any deviations from the plan (e.g., if Green fill looked wrong and Yellow was substituted)
- Screenshot path if the user captured one during verify
</output>
